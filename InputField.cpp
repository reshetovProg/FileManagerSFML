#include "InputField.h"

InputField::InputField(sf::Vector2f size, sf::Vector2f position, sf::Color colorBack, sf::Color colorFont, int fontSize)
	:Field(size, position, colorBack,colorFont,fontSize)
{
	
}
